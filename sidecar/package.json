{
  "name": "bytro-community-sidecar",
  "private": true,
  "version": "0.1.0",
  "license": "Apache-2.0",
  "packageManager": "npm@10.9.7",
  "engines": {
    "node": ">=20",
    "npm": ">=10"
  },
  "type": "module",
  "bin": {
    "skills": "./dist/skills-cli.js"
  },
  "scripts": {
    "build": "tsc",
    "typecheck": "tsc --noEmit",
    "build:bundle": "tsc && esbuild dist/index.js --bundle --platform=node --target=node20 --format=esm --outfile=dist/bundle.mjs --minify --sourcemap --banner:js=\"import { createRequire } from 'module'; const require = createRequire(import.meta.url);\" && npm run build:openai-images-mcp",
    "build:openai-images-mcp": "esbuild src/openai-images-mcp-entry.ts --bundle --platform=node --target=node20 --format=esm --outfile=dist/openai-images-mcp.mjs --banner:js=\"import { createRequire } from 'module'; const require = createRequire(import.meta.url);\"",
    "build:skills-cli": "tsc && esbuild dist/skills-cli.js --bundle --platform=node --target=node20 --format=esm --outfile=dist/skills-cli.mjs --banner:js=\"#!/usr/bin/env node\"",
    "test": "vitest run",
    "dev": "tsc --watch"
  },
  "dependencies": {
    "@anthropic-ai/claude-agent-sdk": "0.3.220",
    "@modelcontextprotocol/sdk": "^1.30.0",
    "openai": "^7.2.0",
    "zod": "^4.4.3"
  },
  "overrides": {
    "@hono/node-server": "2.0.11",
    "body-parser": "2.3.0",
    "fast-uri": "4.1.1",
    "hono": "4.12.32"
  },
  "binaryVersions": {
    "_comment": "Claude is derived from dependencies[\"@anthropic-ai/claude-agent-sdk\"]; Codex uses this explicit pin.",
    "codex": "0.144.4"
  },
  "devDependencies": {
    "@types/node": "^26.1.2",
    "esbuild": "^0.28.1",
    "typescript": "~7.0.2",
    "vitest": "^4.0.18"
  }
}
